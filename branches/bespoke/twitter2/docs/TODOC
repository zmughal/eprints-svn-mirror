


Stuff to document. Ignore it.
 
* render_opts!!!
* magick_stops!!!
per-page-validation!!!
per-page-titles!!!
$c->{field_defaults}->{hide_honourific} = 0;!!
$c->{field_defaults}->{hide_lineage} = 0;!!
$c->{field_defaults}->{family_first} = 0;!!

view: subpages, subheadings, start_headign, render_opts
citations: render_opts
* min_resolution, res=year!!!


Todoc: field render attrs (metadataconfig, view, citation),gf,fg
* render_input
* latest_tool
* input_style="long"
* get_users_owned_eprints
* field_defaults
* help_per_type in eprints 
* spans in citations
* field_defaults
"SIMPLE" searches

-
MimeTex support
*Filters*
Filters in OAI
Filters in search
Filters in latest_tool
New search config.
New subject input mode.
register_agreement for register page.
apache 2.
var directory

NEW SystemSettings
NEW Archive.XML config

search for all XX.
