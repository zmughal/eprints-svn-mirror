EPrints RAE Module - BRONZE RELEASE

What does the RAE plugin do?

- Enter measures of esteem (optionally on behalf of another user)
- Search for publications (optionally on behalf of another user)
- Add publications from search results to "selected" list (optionally on behalf of another user)
- Qualify each selected publication for RAE return (optionally on behalf of another user)
- View/export reports (Microsoft Word, Microsoft Excel)

What doesn't the RAE plugin do?

- Optimise your RAE return
- Produce a HEFCE upload

Infrastructure that you should probably have in place

- Organisation structure, added to users
- Unique IDs for users

INSTALLATION

1. Create database tables

create_rae_tables

2. Install scripts

foreach my $file (
        "cfg/ArchiveOAIConfig.pm",
        "cfg/ArchiveRenderConfig.pm",
        "cfg/ArchiveValidateConfig.pm",
        "cfg/ArchiveTextIndexingConfig.pm",
        "cfg/ArchiveMetadataFieldsConfig.pm",
        "cfg/ArchiveRAEConfig.pm" )


sub get_entities
{
        my( $archive, $langid ) = @_;

        my %entities = ();
        $entities{archivename} = $archive->get_conf( "archivename", $langid );
        $entities{adminemail} = $archive->get_conf( "adminemail" );
        $entities{base_url} = $archive->get_conf( "base_url" );
        $entities{perl_url} = $archive->get_conf( "perl_url" );
        $entities{frontpage} = $archive->get_conf( "frontpage" );
        $entities{userhome} = $archive->get_conf( "userhome" );
        $entities{version} = EPrints::Config::get( "version" );
        $entities{ruler} = EPrints::XML::to_string( $archive->get_ruler() );
        $entities{rae_phrases} = "SYSTEM phrases-rae-" . $langid . ".xml";

        return %entities;
}



Copy all files to /opt/eprints2/cgi/users/rae/.

3. Configuration

Copy the cfg/rae-phrases-en.xml and cfg/ArchiveRAEConfig.pm to the /opt/eprints2/archives/ARCHIVEID/cfg/ directory.

These configuration settings are based on those currently used by the University of Southampton Eprints repository and are provided as a starting point. You will need to adjust these settings to suit your EPrints setup.
