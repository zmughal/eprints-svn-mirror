Non-privileged port Apache for individual users

 - Export this directory to ~/eprints/apache/ (where ~/eprints/ is the system
   path of EPrints)

 - Append bashrc to ~/.bashrc:
	$ cat bashrc >>~/.bashrc

 - Start Apache:
	apache2ctl start
